version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: smartapply-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: smartapply
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: infra/Dockerfile
      target: development
    container_name: smartapply-api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/smartapply
      JWT_SECRET: DOCKER_DEV_SECRET_NOT_FOR_PRODUCTION_GENERATE_WITH_OPENSSL_RAND_BASE64_64_FOR_REAL_USE
      NODE_ENV: development
      PORT: 3000
      STORAGE_DRIVER: disk
      LLM_PROVIDER: mock
    ports:
      - "3000:3000"
    volumes:
      - ./apps:/app/apps
      - ./prompts:/app/prompts
      - ./storage:/app/storage
      - /app/node_modules
      - /app/apps/api/node_modules
    command: npm run start:dev
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/v1/health/ready"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

volumes:
  postgres_data:
