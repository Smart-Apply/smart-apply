// Removed: AzureServiceBusProvider was deleted as part of MVP simplification.
// Delete this file from disk:
// rm apps/api/src/jobs/__tests__/unit/providers/azure-service-bus.provider.spec.ts
describe('AzureServiceBusProvider (removed)', () => {
  it.skip('was removed in favor of in-memory + QStash queue providers', () => {
    // intentionally empty
  });
});

